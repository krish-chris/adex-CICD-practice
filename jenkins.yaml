pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/krish-chris/adex-CICD-practice.git'
            }
        }
        stage('build'){
          steps{
            bat "python 1st.py"
          }
        }

    }
}
