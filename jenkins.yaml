pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/krish-chris/adex-CICD-practice.git'
            }
        }
        stage('build'){
          bat "python 1st.py"
        }
    }
}
